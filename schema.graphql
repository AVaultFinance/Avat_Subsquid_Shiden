type TVLChart @entity {
  id: ID!
  currentTimestamp: BigInt!
  endTimestamp: BigInt!
  aLpAmount: Float!
  lpPrice: [LpPrice!]!
  block: Int!
}

type LpPrice {
  id: ID!
  tokenSymbol: String!
  price: Float!
}
